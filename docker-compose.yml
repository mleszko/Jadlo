services:
  jadlo-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jadlo-route-planner
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHON_VERSION=3.12
    restart: unless-stopped
    volumes:
      # Mount cache directory for OSM data persistence
      - ./cache:/app/cache
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          # Limit memory usage (adjust based on instance size)
          # Oracle Cloud free tier: up to 24GB available
          # Set to 24G to utilize full Oracle Cloud free tier capacity
          # For large routes (100km+), full 24GB allows better performance
          memory: 24G
        reservations:
          memory: 4G
